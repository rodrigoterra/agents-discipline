{
  "name": "screenshot-annotator",
  "version": "1.1.0",
  "description": "Screenshot annotation MCP server + browser canvas for Claude Code",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:server": "node build.mjs server",
    "build:webapp": "node build.mjs webapp",
    "dev": "node mcp-server/dist/dev-serve.js",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.mjs",
    "test:browser": "node --test test/browser.spec.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "playwright": "^1.60.0",
    "typescript": "^5.6.0"
  }
}
