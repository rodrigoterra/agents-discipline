# Python sidecar dependencies for the space-tracking web service.
# Kept separate from requirements.txt so the curses game's runtime stays lean.
#
# Install:  pip install -r requirements-sidecar.txt
#
# Licenses: skyfield (MIT), pyshp (MIT), requests (Apache-2.0),
#           FastAPI (MIT), uvicorn (BSD-3), websockets (BSD-3).
# Nothing copyleft here — safe to combine with the MIT-licensed app.

skyfield>=1.49
pyshp>=2.3.1
requests>=2.31.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
