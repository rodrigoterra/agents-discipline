# CLAUDE.local.md — personal overrides (gitignored)

> Your voice, your shortcuts, your local quirks. Never committed.

## Voice / tone
- <e.g. "Default to concise. No emojis. No sycophantic intros.">

## Tooling preferences
- Always run with `<pnpm | yarn | npm>`.
- Prefer `<jest | vitest>` for new tests.
- Open files in `<editor>` when proposing diffs.

## Local commands I run
- `<command>` — <what it does>

## Things I never want auto-approved
- `<command pattern>`

## Things I always want auto-approved (also wire into `.claude/settings.local.json`)
- `<command pattern>`
